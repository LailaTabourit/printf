#include "main.h"

/**
* _print_string - prints a strings
* @str: string
*
* by : lailitaa & Megatoo
* Return : number of characters printed
*/

int _print_string(char *str)
{
	int i, cmp = 0;

	for (i = 0; str && str[i]; i++)
	
		if
